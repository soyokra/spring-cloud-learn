eureka:
  client:
    serviceUrl:
      register-with-eureka: true
      fetch-registry: true
      defaultZone: http://127.0.0.1:8090/eureka/

spring:
  profiles:
    active: dev

---
spring:
  profiles: prod
  application:
    name: app-product-server
  cloud:
    config:
      label: app-product-server/prod
      discovery:
        enabled: true
        service-id: app-config-server


---
spring:
  profiles: dev
  application:
    name: app-product-server
  cloud:
    config:
      label: app-product-server/dev
      discovery:
        enabled: true
        service-id: app-config-server